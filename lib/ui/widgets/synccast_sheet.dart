import 'package:flutter/material.dart';
import '../screens/jam_studio_sheet.dart';

class SyncCastSheet extends StatelessWidget {
  const SyncCastSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const JamStudioSheet();
  }
}
